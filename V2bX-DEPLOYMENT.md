# V2bX 对接部署指南

> 基于 Cloudflare Workers 的 V2bX 节点端实现  
> 对接 V2board 后端，支持流量统计、用户管理、智能代理

---

## 📋 目录

- [功能特性](#功能特性)
- [架构说明](#架构说明)
- [前置要求](#前置要求)
- [快速部署](#快速部署)
- [配置说明](#配置说明)
- [V2board 后端配置](#v2board-后端配置)
- [测试验证](#测试验证)
- [故障排查](#故障排查)
- [API 说明](#api-说明)

---

## ✨ 功能特性

### 核心功能
- ✅ **V2board API 对接** - 完整支持 V2bX 后端 API
- ✅ **多协议支持** - VLESS、VMess、Trojan
- ✅ **流量统计** - 实时上报用户流量数据
- ✅ **用户管理** - 自动同步用户列表
- ✅ **智能代理** - 地理位置智能匹配
- ✅ **订阅生成** - 自动生成客户端订阅链接

### 技术特性
- ⚡ **边缘计算** - Cloudflare 全球 CDN 加速
- 🔒 **安全可靠** - UUID 实时验证
- 📊 **流量上报** - 批量上报优化性能
- 🌍 **智能路由** - 15+ 地区智能匹配
- 💾 **配置缓存** - 60秒缓存减少 API 请求

---

## 🏗️ 架构说明

```
┌─────────────────┐         ┌─────────────────┐
│  V2board 后端    │◄────────│  CF Worker      │
│  (V2bX API)     │  HTTP    │  节点端          │
│                 │         │                 │
│  - 用户管理      │         │  - 流量转发      │
│  - 节点配置      │         │  - 订阅生成      │
│  - 流量统计      │         │  - 智能代理      │
└─────────────────┘         └─────────────────┘
                                    │
                                    ↓
                            ┌──────────────┐
                            │  V2Ray 客户端 │
                            └──────────────┘
```

### API 交互流程

1. **配置同步**
   ```
   Worker → V2board: GET /api/v1/server/UniProxy/config
   Worker → V2board: GET /api/v1/server/UniProxy/user
   ```

2. **流量上报**
   ```
   Worker → V2board: POST /api/v1/server/UniProxy/push
   数据格式: { user_id: [upload, download] }
   ```

3. **订阅生成**
   ```
   Client → Worker: GET /{uuid}
   Worker → Client: Base64(VLESS Links)
   ```

---

## 📦 前置要求

### 必需条件
1. ✅ **Cloudflare 账号** - 免费版即可
2. ✅ **V2board 后端** - 已部署并运行的 V2bX 后端
3. ✅ **API 密钥** - V2board 后端的 API Token
4. ✅ **节点 ID** - 在 V2board 后端创建的节点 ID

### 可选条件
- 🌐 自定义域名（推荐，用于更好的连接稳定性）
- 🔧 反代 IP 列表（用于智能代理）

---

## 🚀 快速部署

### 第一步：在 V2board 后端创建节点

1. 登录 V2board 管理后台
2. 进入 **节点管理** → **添加节点**
3. 填写节点信息：
   - **节点名称**: `Cloudflare Workers 节点`
   - **节点类型**: `VLESS` / `VMess` / `Trojan`
   - **协议**: `WebSocket`
   - **端口**: `443`
   - **TLS**: 启用
   - **传输方式**: `ws`
   - **路径**: `/?ed=2048`
4. 保存并记录 **节点 ID**

### 第二步：获取 API 密钥

1. 在 V2board 后台找到 **系统设置** → **API 设置**
2. 生成或复制 **API Key (Token)**
3. 记录 **后端 API 地址**（例如：`https://your-v2board.com`）

### 第三步：部署 Cloudflare Worker

#### 3.1 创建 Worker

1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 点击 **Workers & Pages**
3. 点击 **Create application** → **Create Worker**
4. Worker 名称输入：`v2bx-node`（或其他名称）
5. 点击 **Deploy**

#### 3.2 编辑 Worker 代码

1. 点击 **Edit code**
2. 删除默认代码
3. 复制 `V2bX-Node-Worker.js` 的全部内容
4. 粘贴到编辑器

#### 3.3 修改配置参数

找到代码顶部的配置区域（第 7-13 行），修改为你的实际信息：

```javascript
const V2BOARD_CONFIG = {
    apiHost: 'https://your-v2board-domain.com',  // ← 修改为你的 V2board 后端地址
    apiKey: 'your-api-key-here',                  // ← 修改为你的 API Key
    nodeId: 1,                                     // ← 修改为你的节点 ID
    nodeType: 'vless'                              // ← 节点类型: vless, vmess, trojan
};
```

**配置示例：**
```javascript
const V2BOARD_CONFIG = {
    apiHost: 'https://api.example.com',
    apiKey: 'sk_1234567890abcdef',
    nodeId: 5,
    nodeType: 'vless'
};
```

#### 3.4 保存并部署

1. 点击 **Save and deploy**
2. 等待部署完成

#### 3.5 绑定自定义域名（推荐）

1. 在 Worker 详情页，点击 **Settings** 标签
2. 找到 **Triggers** → **Custom Domains**
3. 点击 **Add Custom Domain**
4. 输入域名：`v2bx-node.yourdomain.com`
5. 等待 SSL 证书配置完成

✅ **部署完成！**

---

## ⚙️ 配置说明

### 配置参数详解

#### V2BOARD_CONFIG
```javascript
{
    apiHost: 'https://your-v2board.com',  // V2board 后端 API 地址（不要带尾随斜杠）
    apiKey: 'your-api-key',                // API Token（从 V2board 后台获取）
    nodeId: 1,                             // 节点 ID（整数）
    nodeType: 'vless'                      // 节点类型：vless, vmess, trojan
}
```

#### 节点类型说明

| 类型 | 协议 | 特点 |
|------|------|------|
| `vless` | VLESS | 推荐，轻量级，性能最佳 |
| `vmess` | VMess | 兼容性好，支持老客户端 |
| `trojan` | Trojan | 伪装性强，适合敏感环境 |

#### FALLBACK_CONFIG (兜底配置)
```javascript
{
    proxyIPs: ['bestproxy.030101.xyz:443'],  // 当无法连接 V2board 时使用
    bestDomains: ['bestcf.030101.xyz:443']
}
```

#### CACHE_TTL (缓存时间)
```javascript
const CACHE_TTL = 60000;  // 60秒，减少 API 请求频率
```

---

## 🔧 V2board 后端配置

### 节点配置示例

#### VLESS 节点
```json
{
  "host": "v2bx-node.yourdomain.com",
  "server_port": 443,
  "server_name": "v2bx-node.yourdomain.com",
  "network": "ws",
  "network_settings": {
    "path": "/?ed=2048",
    "headers": {
      "Host": "v2bx-node.yourdomain.com"
    }
  },
  "tls": 1,
  "flow": ""
}
```

#### VMess 节点
```json
{
  "host": "v2bx-node.yourdomain.com",
  "server_port": 443,
  "server_name": "v2bx-node.yourdomain.com",
  "network": "ws",
  "network_settings": {
    "path": "/?ed=2048",
    "headers": {
      "Host": "v2bx-node.yourdomain.com"
    }
  },
  "tls": 1
}
```

### 路由配置（智能代理）

在 V2board 后台的节点路由中添加：

```json
{
  "action": "proxy",
  "action_value": "ProxyIP.HK.example.com:443"
}
```

**支持多个代理：**
```json
[
  {
    "action": "proxy",
    "action_value": "ProxyIP.HK.example.com:443"
  },
  {
    "action": "proxy",
    "action_value": "ProxyIP.JP.example.com:443"
  },
  {
    "action": "proxy",
    "action_value": "ProxyIP.US.example.com:443"
  }
]
```

---

## 🧪 测试验证

### 1. 健康检查

访问 Worker 根路径：
```
https://v2bx-node.yourdomain.com/
```

正常返回：
```html
✅ V2bX Node Worker Running
Connecting to V2board backend
```

### 2. 调试接口

访问调试接口查看配置：
```
https://v2bx-node.yourdomain.com/debug
```

返回示例：
```json
{
  "users": [
    "uuid-1",
    "uuid-2",
    "uuid-3"
  ],
  "userCount": 3,
  "nodeInfo": {
    "host": "v2bx-node.yourdomain.com",
    "server_port": 443,
    "network": "ws",
    "tls": 1
  },
  "lastUpdate": "2025-11-22T10:30:00.000Z",
  "v2boardUrl": "https://your-v2board.com"
}
```

### 3. 订阅测试

在 V2board 用户面板获取订阅链接，格式为：
```
https://v2bx-node.yourdomain.com/{user-uuid}
```

使用浏览器访问，应该返回 Base64 编码的订阅内容。

### 4. 客户端测试

1. 使用 V2Ray 客户端（如 v2rayN、v2rayNG）
2. 添加订阅：输入用户的订阅链接
3. 更新订阅
4. 选择节点并测试连接

---

## 🔍 故障排查

### 问题 1：订阅链接返回 404

**原因：**
- Worker 无法连接到 V2board 后端
- UUID 不在用户列表中
- 配置参数错误

**解决方法：**
1. 访问 `/debug` 接口检查配置
2. 确认 `apiHost`、`apiKey`、`nodeId` 是否正确
3. 检查 V2board 后端是否正常运行
4. 检查用户 UUID 是否在 `users` 列表中

### 问题 2：流量不更新

**原因：**
- 流量上报失败
- API Token 权限不足
- 网络连接问题

**解决方法：**
1. 检查 Worker 日志（在 Cloudflare Dashboard 查看）
2. 确认 API Token 有流量上报权限
3. 测试 V2board API 端点是否可访问

### 问题 3：节点连接失败

**原因：**
- 节点配置错误
- TLS 配置不匹配
- 路径配置错误

**解决方法：**
1. 检查 V2board 节点配置中的 `host`、`port`、`path`
2. 确认 TLS 设置为 1（启用）
3. 确认客户端配置与服务端一致

### 问题 4：缓存未更新

**原因：**
- 缓存时间未到期
- 强制刷新间隔限制

**解决方法：**
1. 等待 60 秒后自动刷新
2. 重启 Worker（重新部署）
3. 修改 `CACHE_TTL` 为更短的时间

---

## 📡 API 说明

### V2board API 端点

#### 1. 获取节点配置
```
GET /api/v1/server/UniProxy/config
Query Parameters:
  - node_type: vless|vmess|trojan
  - node_id: 节点ID
  - token: API Token

Response:
{
  "host": "domain.com",
  "server_port": 443,
  "network": "ws",
  "network_settings": {},
  "tls": 1,
  ...
}
```

#### 2. 获取用户列表
```
GET /api/v1/server/UniProxy/user
Query Parameters:
  - node_type: vless|vmess|trojan
  - node_id: 节点ID
  - token: API Token

Response:
{
  "users": [
    {
      "id": 1,
      "uuid": "uuid-string",
      "speed_limit": 0,
      "device_limit": 0
    }
  ]
}
```

#### 3. 上报流量
```
POST /api/v1/server/UniProxy/push
Query Parameters:
  - node_type: vless|vmess|trojan
  - node_id: 节点ID
  - token: API Token

Body (JSON):
{
  "1": [1024000, 2048000],  // [上传, 下载] (字节)
  "2": [512000, 1024000]
}
```

---

## 🎯 高级配置

### 智能代理配置

在代理地址中包含地区标识，系统会自动智能选择：

```javascript
proxyIPs: [
    'ProxyIP.HK.example.com:443',  // 香港代理
    'ProxyIP.JP.example.com:443',  // 日本代理
    'ProxyIP.US.example.com:443',  // 美国代理
    'ProxyIP.SG.example.com:443'   // 新加坡代理
]
```

**工作原理：**
- 用户访问 `japan.com` → 优先使用日本代理
- 用户访问 `hongkong.com` → 优先使用香港代理
- 无法识别地区时 → 按顺序尝试所有代理

### Reality 支持（VLESS）

如果 V2board 节点配置了 Reality：

```json
{
  "tls": 2,
  "tls_settings": {
    "public_key": "your-public-key",
    "short_id": "your-short-id",
    "server_name": "www.microsoft.com"
  }
}
```

Worker 会自动生成 Reality 订阅链接。

---

## 📊 性能优化

### 1. 缓存策略
- 节点配置缓存 60 秒
- 用户列表缓存 60 秒
- 减少 API 请求频率

### 2. 流量上报优化
- 批量上报（每 10 个连接）
- 异步上报（不阻塞主流程）
- 聚合相同用户的流量

### 3. 连接策略
- 优先直连
- 智能代理重试
- 地理位置匹配

---

## 🔐 安全建议

1. ✅ **使用自定义域名** - 避免暴露 `.workers.dev` 域名
2. ✅ **定期更换 API Key** - 提高安全性
3. ✅ **限制节点访问** - 在 V2board 设置流量限制
4. ✅ **监控异常流量** - 及时发现异常使用
5. ✅ **启用 TLS** - 确保传输加密

---

## 📝 更新日志

### v1.0.0 (2025-11-22)
- ✅ 初始版本发布
- ✅ 支持 V2bX API 对接
- ✅ 支持 VLESS、VMess、Trojan
- ✅ 流量统计与上报
- ✅ 智能地理位置匹配
- ✅ 订阅生成功能

---

## 🤝 技术支持

- GitHub Issues: [提交问题](https://github.com/kiftyxx/vles/issues)
- V2bX 文档: [https://github.com/wyx2685/V2bX](https://github.com/wyx2685/V2bX)

---

## 📄 许可证

MIT License

---

**最后更新：** 2025-11-22  
**版本：** v1.0.0  
**作者：** kiftyxx
